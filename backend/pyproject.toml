[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "email-validator>=2.3.0",
    "faker>=40.37.0",
    "numpy>=2.1,<2.3",
    "polars>=1.44.0",
    "pydantic>=2.13.4",
    "phonenumbers>=9.0.26",
    "rapidfuzz>=3.14.3",
    "torch>=2.11.0",
    "torchaudio>=2.11.0",
    "torchvision>=0.26.0",
    "neo4j>=6.0.0",
    "shap>=0.48,<0.49",
    "catboost>=1.2.10",
    "scikit-learn>=1.9,<1.10",
    "langchain-core>=1.5,<1.6",
    "langchain>=1.3.15",
    "langchain-openai>=1.4.3",
    "openai>=2.54,<3",
    "sqlalchemy>=2.0,<2.1",
    "psycopg[binary]>=3.2,<3.4",
    "alembic>=1.19,<1.20",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
    "python-dotenv>=1.0,<2",
]

[tool.uv.sources]
torch = { index = "pytorch-cu128" }
torchvision = { index = "pytorch-cu128" }
torchaudio = { index = "pytorch-cu128" }

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"

[dependency-groups]
dev = [
    "basedpyright>=1.39.10",
    "deptry>=0.25.1",
    "ruff>=0.16.4",
    "vulture>=2.16",
]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM", "RUF", "DTZ"]

[tool.basedpyright]
include = ["main.py", "mayajaal", "tests"]
exclude = [".venv"]
venvPath = "."
venv = ".venv"
pythonVersion = "3.12"
typeCheckingMode = "strict"

[tool.deptry]
known_first_party = ["mayajaal"]

# These are deliberately retained for future representation-learning work.
# All other missing, transitive, misplaced, and unused dependencies still fail.
[tool.deptry.per_rule_ignores]
DEP002 = [
    "faker",
    "numpy",
    "polars",
    "psycopg", # SQLAlchemy's explicit postgresql+psycopg runtime driver
    "scikit-learn", # dynamically imported only in mayajaal.interop.sklearn
    "torch",
    "torchaudio",
    "torchvision",
    "uvicorn", # `just api-run` invokes the ASGI server as a CLI entry point
]
